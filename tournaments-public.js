import { supabase, safe } from './app.js';const box=document.getElementById('tournamentsBox');async function load(){const {data,error}=await supabase.from('tournaments').select('*').order('start_date',{ascending:true,nullsFirst:false});if(error){box.innerHTML=`<div class="card"><h2>Could not load</h2><p class="muted">${safe(error.message)}</p></div>`;return}if(!data?.length){box.innerHTML='<div class="card"><h2>No tournaments yet</h2><p class="muted">Tournaments appear here once staff creates them.</p></div>';return}box.innerHTML=data.map(t=>`<article class="card row"><div><span class="pill">${safe(t.status||'upcoming')}</span><h2>${safe(t.name||'VCC Tournament')}</h2><p class="muted">${safe(t.description||'')}</p><p><strong>Category:</strong> ${safe(t.tournament_category||'Open')} · <strong>Format:</strong> ${safe(t.format||'TBD')}</p><p><strong>Starts:</strong> ${t.start_date?new Date(t.start_date).toLocaleString():'TBD'} · <strong>Roster Lock:</strong> ${t.roster_lock_at?new Date(t.roster_lock_at).toLocaleString():'TBD'}</p></div><a class="btn" href="matches.html">View Matches</a></article>`).join('')}load();