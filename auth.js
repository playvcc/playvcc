import { supabase } from './app.js';login.onclick=async()=>{const {error}=await supabase.auth.signInWithPassword({email:email.value,password:pass.value});status.textContent=error?'Error: '+error.message:'Signed in.';if(!error)setTimeout(()=>location.href='profile.html',800)};signup.onclick=async()=>{const {error}=await supabase.auth.signUp({email:email.value,password:pass.value});status.textContent=error?'Error: '+error.message:'Account created. Check email if needed.'};