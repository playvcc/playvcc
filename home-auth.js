(function(){function openAuth(mode){location.href='auth.html?mode='+encodeURIComponent(mode||'login')} function wire(){document.querySelectorAll('#loginOpen,[data-auth="login"],.auth-login').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();openAuth('login')}));document.querySelectorAll('#signupOpen,[data-auth="signup"],.auth-signup').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();openAuth('signup')}));document.querySelectorAll('a[href="profile.html"],a[href="./profile.html"]').forEach(link=>link.addEventListener('click',async e=>{try{const c=await import('./supabase-config.js');const l=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm');const s=l.createClient(c.SUPABASE_URL,c.SUPABASE_ANON_KEY);const session=await s.auth.getSession();if(!session?.data?.session?.user){e.preventDefault();openAuth('login')}}catch(err){e.preventDefault();openAuth('login')}}))} if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire()})();
