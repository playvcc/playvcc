import { supabase, safe } from './app.js';let grouped=[];function kd(k,d){return d>0?k/d:k}async function loadStats(){const {data,error}=await supabase.from('player_match_stats').select('*');if(error){teamRows.innerHTML=`<div class="stats-empty">${safe(error.message)}</div>`;return}const map=new Map();(data||[]).forEach(r=>{const key=(r.team_id||r.team_name||'Free Agent').toLowerCase();if(!map.has(key))map.set(key,{team_name:r.team_name||'Free Agent',maps:0,wins:0,losses:0,kills:0,deaths:0,acsTotal:0});const t=map.get(key);t.maps++;if((r.result||'').toLowerCase()==='win')t.wins++;if((r.result||'').toLowerCase()==='loss')t.losses++;t.kills+=Number(r.kills||0);t.deaths+=Number(r.deaths||0);t.acsTotal+=Number(r.acs||0)});grouped=[...map.values()].map(t=>({...t,kd:kd(t.kills,t.deaths),acs:t.maps?Math.round(t.acsTotal/t.maps):0}));render()}function render(){let data=grouped.filter(t=>{const q=searchInput.value.toLowerCase();if(q&&!t.team_name.toLowerCase().includes(q))return false;if(resultFilter.value==='winning'&&t.wins<t.losses)return false;return true});data.sort((a,b)=>sortBy.value==='kd'?b.kd-a.kd:sortBy.value==='acs'?b.acs-a.acs:sortBy.value==='maps'?b.maps-a.maps:b.wins-a.wins);if(!data.length){teamRows.innerHTML='<div class="stats-empty">No approved team stats yet.</div>';return}teamRows.innerHTML=data.map((t,i)=>`<div class="stats-row team-stats-row"><span>${i+1}</span><span><a class="team-name" href="teams.html">${safe(t.team_name)}</a><small class="team-sub">${t.wins}-${t.losses}</small></span><span>${t.maps}</span><span>${t.wins}</span><span>${t.losses}</span><span>${t.kills}</span><span class="${t.kd>=1.1?'stat-good':t.kd>=1?'stat-warn':'stat-bad'}">${t.kd.toFixed(2)}</span><span>${t.acs}</span></div>`).join('')}searchInput.oninput=render;sortBy.onchange=render;resultFilter.onchange=render;loadStats();