import { supabase, safe } from './app.js';async function load(){const {data,error}=await supabase.from('profiles').select('*').order('pro_points',{ascending:false}).limit(50);if(error){box.innerHTML='<div class="card">'+safe(error.message)+'</div>';return}box.innerHTML=data?.length?data.map((p,i)=>`<article class="card row"><div><h2>#${i+1} ${safe(p.display_name||p.username||'Player')}</h2><p><span class="pill">${safe(p.rank||'Unranked')}</span><span class="pill">Points: ${safe(p.pro_points||0)}</span></p></div><a class="btn" href="profile.html?user=${p.id}">View</a></article>`).join(''):'<div class="card">No rankings yet.</div>'}load();