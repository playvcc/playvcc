import { supabase } from './app.js';const $=id=>document.getElementById(id);const params=new URLSearchParams(location.search);const requestedUserId=params.get('user')||'';async function getUser(){const s=await supabase.auth.getSession();return s?.data?.session?.user||null}async function loadProfileById(id){const {data,error}=await supabase.from('profiles').select('*').eq('id',id).maybeSingle();if(error&&error.code!=='PGRST116')throw error;return data}async function getCaptainTeam(id){const {data}=await supabase.from('teams').select('*').eq('captain_id',id).maybeSingle();return data}function setText(id,v){if($(id))$(id).textContent=v??''}function setValue(id,v){if($(id))$(id).value=v||''}function showPublic(){editProfileControls.classList.add('hidden');editImageControls.classList.add('hidden');publicProfileView.classList.remove('hidden');messagesPanel.classList.add('hidden');profileTitle.textContent='VCC Player Profile';profileModeNotice.textContent='Public view only.'}function showEdit(){editProfileControls.classList.remove('hidden');editImageControls.classList.remove('hidden');publicProfileView.classList.add('hidden');messagesPanel.classList.remove('hidden');profileTitle.textContent='My VCC Profile';profileModeNotice.textContent='Edit your own profile.'}function fillPublic(p){setText('publicName',p?.display_name||p?.username||'Player');setText('publicRiot',p?.riot_id||'Riot ID not set');setText('publicRank',p?.rank||'Unranked');setText('publicPlatform',p?.platform||'Console');setText('publicRole',p?.main_role||p?.role||'Flex');setText('publicAgents',p?.main_agents||'Not set');setText('publicBio',p?.bio||'No bio yet.');if(p?.avatar_url)profileAvatar.src=p.avatar_url;setText('winsText',p?.wins||0);setText('lossesText',p?.losses||0);setText('pointsText',p?.pro_points||0);setText('ratingText',p?.rating||1000);messageBtn.href=`message-player.html?user=${encodeURIComponent(p?.id||'')}&email=${encodeURIComponent(p?.email||'')}`}function fillEdit(p){setValue('displayName',p?.display_name||p?.username||'');setValue('riotId',p?.riot_id||'');setValue('rank',p?.rank||'');setValue('platform',p?.platform||'');setValue('region',p?.region||'');setValue('mainRole',p?.main_role||p?.role||'');setValue('mainAgents',p?.main_agents||'');setValue('bio',p?.bio||'');if(p?.avatar_url)profileAvatar.src=p.avatar_url;setText('winsText',p?.wins||0);setText('lossesText',p?.losses||0);setText('pointsText',p?.pro_points||0);setText('ratingText',p?.rating||1000)}async function ensureProfile(u){let p=await loadProfileById(u.id);if(p)return p;const username=u.email?u.email.split('@')[0]:'VCC Player';const {data,error}=await supabase.from('profiles').insert({id:u.id,email:u.email,username,display_name:username,updated_at:new Date().toISOString()}).select().single();if(error)throw error;return data}async function addInvite(viewed,u){const team=u?await getCaptainTeam(u.id):null;if(!team||!viewed?.id||viewed.id===u.id)return;const b=document.createElement('button');b.className='gold';b.textContent='Invite To My Team';b.onclick=async()=>{const msg=await supabase.from('player_messages').insert({recipient_user_id:viewed.id,recipient_email:viewed.email||null,sender_user_id:u.id,message_type:'team_invite',title:`Invite to ${team.name}`,body:`You have been invited to join ${team.name} on VCC.`,related_team_id:team.id,status:'unread',action_url:'inbox.html'}).select().single();if(msg.error){profileStatus.textContent='Invite error: '+msg.error.message;return}const inv=await supabase.from('team_invites').insert({team_id:team.id,invited_user_id:viewed.id,invited_email:viewed.email||null,invited_by:u.id,role:'player',status:'pending',message_id:msg.data.id});profileStatus.textContent=inv.error?'Invite error: '+inv.error.message:'Invite sent.'};messageBtn.after(b)}async function load(){try{const u=await getUser();if(requestedUserId&&(!u||requestedUserId!==u.id)){showPublic();let p=await loadProfileById(requestedUserId);if(!p)p={id:requestedUserId,display_name:params.get('name')};fillPublic(p);await addInvite(p,u);profileStatus.textContent='Viewing public profile.';return}if(!u){showEdit();profileStatus.innerHTML='You are not logged in.<br><br><a class="btn" href="auth.html">Go to Login</a>';return}showEdit();const p=await ensureProfile(u);fillEdit(p);messageBtn.href=`message-player.html?user=${u.id}&email=${u.email||''}`;profileStatus.textContent='Logged in as '+(u.email||u.id)}catch(e){profileStatus.textContent='Profile error: '+e.message}}async function saveProfile(){try{const u=await getUser();if(!u){profileStatus.textContent='Log in first.';return}const dn=document.getElementById('displayName').value.trim();const patch={id:u.id,email:u.email,username:dn||(u.email?u.email.split('@')[0]:'VCC Player'),display_name:dn,riot_id:riotId.value.trim(),rank:rank.value.trim(),platform:platform.value.trim(),region:region.value.trim(),main_role:mainRole.value.trim(),role:mainRole.value.trim(),main_agents:mainAgents.value.trim(),bio:bio.value.trim(),updated_at:new Date().toISOString()};const {error}=await supabase.from('profiles').upsert(patch);if(error)throw error;profileStatus.textContent='Profile saved.'}catch(e){profileStatus.textContent='Save error: '+e.message}}async function uploadAvatar(){try{const u=await getUser();if(!u){profileStatus.textContent='Log in first.';return}const file=avatarFile.files[0];if(!file){profileStatus.textContent='Choose image first.';return}const ext=file.name.split('.').pop();const path=`${u.id}/avatar.${ext}`;const up=await supabase.storage.from('profile-images').upload(path,file,{upsert:true});if(up.error)throw up.error;const url=supabase.storage.from('profile-images').getPublicUrl(path).data.publicUrl;const {error}=await supabase.from('profiles').upsert({id:u.id,email:u.email,avatar_url:url,updated_at:new Date().toISOString()});if(error)throw error;profileAvatar.src=url;profileStatus.textContent='Profile picture uploaded.'}catch(e){profileStatus.textContent='Upload error: '+e.message}}document.addEventListener('DOMContentLoaded',()=>{saveProfileBtn.onclick=saveProfile;uploadAvatarBtn.onclick=uploadAvatar;avatarFile.onchange=()=>{const f=avatarFile.files[0];if(f)profileAvatar.src=URL.createObjectURL(f)};load()});